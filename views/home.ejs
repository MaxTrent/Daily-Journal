<%- include('partials/header'); %>

<h1>Home</h1>

<p><%= homeStartingContent %></p>

<%posts.forEach(function(post){%>
    <h1><%= post.title %></h1>
    <p><%= post.content.substr(0, 100) %> &hellip; <a href= "/posts/<%= post.title %>" + post.title >Read More</a></p>
<%});%>

<!-- <%  for (i=0; i<posts.length; i++){%>
    <h1><%= posts[i].title %></h1>
    <p><%= posts[i].content %></p>
<%  }%> -->


<%- include('partials/footer'); %>
